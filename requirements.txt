# --- API backend ---
fastapi
uvicorn[standard]

# --- Interfaz ---
streamlit

# --- Manejo de datos ---
pandas

# --- Generación de datos sintéticos (modo Laboratorio) ---
sdv

# --- Modelo y explicabilidad ---
scikit-learn
xgboost
shap

# --- Ingesta de evidencia (PCAP) ---
scapy

# --- Informe pericial ---
python-docx
matplotlib

# --- Tests ---
pytest

# Nota: el informe pericial .docx se genera con python-docx, ya incluido arriba.
# El repositorio conserva una vía alternativa basada en la librería npm "docx"
# (backend/app/report/scripts/generate_report_docx.js), que la aplicación NO usa
# y que solo requiere Node.js si se invoca generate_report_docx_via_node().
